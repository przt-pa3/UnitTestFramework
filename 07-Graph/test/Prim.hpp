#ifndef _PRIM_H
#define _PRIM_H

#include <optional>
#include <vector>
#include "Graph.hpp"
/* Feel free to add more standard library headers */

/* Returns the vector of edges in the MST, or std::nullopt if MST does
 * not exist */

inline std::optional<edges_t> prim_minimum_spanning_tree(Graph& g, vertex_t src) {
	return {};
}

#endif // _PRIM_H
